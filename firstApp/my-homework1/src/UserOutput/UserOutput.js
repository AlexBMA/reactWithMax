import React from 'react';

const userouput = (props) =>{
    return (
    <div>
        <p>First line user ouput {props.username}</p>
        <p>Secound line user ouput {props.username}</p>
    </div>
)};

export default userouput;